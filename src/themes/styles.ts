import colors from "./colors";

const styles = {
  global: {
    "html, body": {
      color: colors.brand.heavy,
      lineHeight: "tall",
    },

    a: {
      color: "teal.500",
    },

    h1: {
      color: colors.brand.heavy,
    },
  },
};

export default styles;
